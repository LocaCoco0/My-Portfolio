// This function should output n numbers in the 
// fibonacci sequence
function Fib(n) {
  let prevFib1 = 0;
  let prevFib2 = 0;
  let currentFib = 0;

  for (let i = 0; i < n; i++) {
    if (i == 0 || i == 1) {
      currentFib == 1; 
    } else {
      currentFib == prevFib 1 + prevFib 2; 
    }

    console.log(currentFib)
  } else if (i == 2) {
    currentFib == 1;
  }
}